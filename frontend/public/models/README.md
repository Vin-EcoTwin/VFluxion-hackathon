Place a glTF binary named `charging_station.glb` here to enable 3D Scenegraph rendering.

Expected path (URL): `/models/charging_station.glb`

Options:
- Add your own `.glb` file at this path.
- Or provide a public URL and I can update the component to reference it.

If the file is missing, the map falls back to the existing ColumnLayer visualization.